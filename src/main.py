from typing import List

from binary_trees import BSTree


def main():
    return


def bin_tree_combinations(n: int) -> List[List[int]]:
    test_arr = [1, 2, 3, 4, 5, 6]

    for i in range(len(test_arr)):
        head_node = test_arr[i]
        new_arr = test_arr[0:i] + test_arr[i + 1 :]


def create_tree(head_node: int, array: List[int]) -> BSTree:
    return


if __name__ == "__main__":
    main()
